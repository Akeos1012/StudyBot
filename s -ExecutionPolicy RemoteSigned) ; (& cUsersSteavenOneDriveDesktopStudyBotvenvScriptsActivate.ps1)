[1mdiff --git a/app/quiz/generation/question_scorer.py b/app/quiz/generation/question_scorer.py[m
[1mindex 6d737ab..39e7931 100644[m
[1m--- a/app/quiz/generation/question_scorer.py[m
[1m+++ b/app/quiz/generation/question_scorer.py[m
[36m@@ -106,6 +106,7 @@[m [mclass QuestionScorer:[m
         self,[m
         weights: Optional[Dict[str, float]] = None,[m
         min_acceptable_score: float = None,[m
[32m+[m[32m        min_scores: Optional[Dict[str, float]] = None,[m
     ):[m
         """[m
         Initialize the question scorer.[m
[36m@@ -113,6 +114,7 @@[m [mclass QuestionScorer:[m
         Args:[m
             weights: Dictionary of dimension weights. If None, uses defaults.[m
             min_acceptable_score: Minimum score for a question to be acceptable.[m
[32m+[m[32m            min_scores: Minimum scores for individual dimensions.[m
         """[m
         self.weights = weights or DEFAULT_WEIGHTS.copy()[m
         self.min_acceptable_score = ([m
[36m@@ -120,6 +122,7 @@[m [mclass QuestionScorer:[m
             if min_acceptable_score is not None[m
             else settings.DEFAULT_MIN_SCORE[m
         )[m
[32m+[m[32m        self.min_scores = min_scores or {}[m
 [m
     # =========================================================================[m
     # PUBLIC API[m
[36m@@ -186,7 +189,7 @@[m [mclass QuestionScorer:[m
         self, question: Dict[str, Any], facts: Optional[List[Dict[str, Any]]] = None[m
     ) -> Tuple[bool, float, Dict[str, float], List[str]]:[m
         """[m
[31m-        Check if a question meets the quality threshold.[m
[32m+[m[32m        Check if a question meets the quality threshold and dimension floors.[m
 [m
         Args:[m
             question: The question dictionary to check[m
[36m@@ -198,6 +201,14 @@[m [mclass QuestionScorer:[m
         total, scores, issues = self.score_question(question, facts)[m
         is_acceptable = total >= self.min_acceptable_score[m
         [m
[32m+[m[32m        # Check dimension floors (independently of overall threshold)[m
[32m+[m[32m        if self.min_scores:[m
[32m+[m[32m            for dim, floor in self.min_scores.items():[m
[32m+[m[32m                if scores.get(dim, 0) < floor:[m
[32m+[m[32m                    is_acceptable = False[m
[32m+[m[32m                    issues.append(f"Dimension {dim} score {scores.get(dim, 0)} below floor {floor}")[m
[32m+[m[32m                    # No break, collect all floor violations[m
[32m+[m[41m        [m
         if not is_acceptable:[m
             logger.warning([m
                 "Question rejected. Total score: %.2f. Issues: %s. Component Scores: %s. Question: %s",[m
[36m@@ -330,7 +341,7 @@[m [mclass QuestionScorer:[m
             # Neutral instead of harsh penalty.[m
             scores.append(0.8)[m
 [m
[31m-        from .question_grounding import explanation_supported_by_fact[m
[32m+[m[32m        from app.quiz.validation.question_grounding import explanation_supported_by_fact[m
 [m
         explanation = question.get("explanation", "")[m
         supporting_fact = question.get("supporting_fact", "")[m
