**Authentication** –  the **process of verifying a user’s identity before allowing access to a system or resource**.

- Uses credentials like passwords, PINs, biometrics, or OTP
- Happens before [[Authorization]]
- Common in Security systems and applications

Authentication is the process of verifying a user’s identity before granting access to a system or resource.